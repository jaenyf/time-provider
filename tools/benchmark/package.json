{
  "name": "@time-provider/benchmark",
  "private": true,
  "description": "Library benchmark",
  "type": "module",
  "scripts": {
    "bench": "rimraf bench-results && vp test bench bench/clock-read.bench.ts && vp test bench bench/manual-scheduling.bench.ts && vp test bench bench/sequential-scheduling.bench.ts && node bench/shared/report.ts",
    "bench:multi": "node bench/run-multi.ts",
    "bench:report": "node bench/shared/report.ts",
    "bench:changelog": "node bench/generate-benchmark-md.ts"
  },
  "devDependencies": {
    "@jest/fake-timers": "^30",
    "@sinonjs/fake-timers": "^15",
    "@time-provider/addon-idle": "workspace:*",
    "@time-provider/core": "workspace:*",
    "@time-provider/plugin-native": "workspace:*",
    "rimraf": "6.1.3"
  }
}
