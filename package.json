{
  "name": "time-provider",
  "version": "0.0.3",
  "private": true,
  "description": "A TypeScript library for injecting time into your application, making time-dependent code deterministic, easy to test, and independent of your date library.",
  "keywords": [
    "clock",
    "date",
    "deterministic",
    "fake",
    "fixed",
    "frozen",
    "injection",
    "manual",
    "provider",
    "running",
    "system",
    "time"
  ],
  "homepage": "https://github.com/jaenyf/time-provider#readme",
  "bugs": {
    "url": "https://github.com/jaenyf/time-provider/issues"
  },
  "license": "MIT",
  "author": "jaenyf",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jaenyf/time-provider.git"
  },
  "workspaces": [
    "packages/*",
    "apps/*",
    "tools/*"
  ],
  "type": "module",
  "scripts": {
    "ready": "vp run test && vp run build && vp run check && vp run stryker",
    "dev": "vp run website#dev",
    "prepare": "vp config",
    "build": "vp run -r build",
    "test": "vp test",
    "check": "vp run -r check",
    "stryker": "stryker run",
    "bump": "vpx bumpp --recursive",
    "publish": "false && vp run build && vp run bump && vpx npm login && vp run -r publish"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "@vitest/coverage-v8": "4.1.9",
    "vite-plus": "^0.2.2"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "bun@1.3.14",
  "catalog": {
    "vite-plus": "latest"
  }
}
